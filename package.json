{
  "name": "fetch-upload-s3",
  "version": "0.3.2",
  "description": "fetch an asset (a picture for instance) from a remote server and upload it to S3 (and remove the local copy). Or upload a local file on S3",
  "main": "index.js",
  "scripts": {
    "test": "mocha -w --timeout 10000 --reporter Spec"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/saintmac/fetch-upload-s3.git"
  },
  "keywords": [
    "fetch",
    "remote",
    "upload",
    "s3"
  ],
  "author": "Martin Saint-Macary",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/saintmac/fetch-upload-s3/issues"
  },
  "dependencies": {
    "async": "~0.2.9",
    "aws-sdk": "^2.702.0",
    "request": "~2.27.0"
  },
  "devDependencies": {
    "chai": "~1.8.1",
    "mocha": "~1.13.0",
    "rimraf": "~2.2.2"
  }
}
